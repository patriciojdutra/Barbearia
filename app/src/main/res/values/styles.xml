<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:windowFullscreen">true</item>
        <item name="colorAccent">@color/colorPrimary</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="bottombar" parent="AppTheme" >
        <item name="android:colorPrimary">@color/branco</item>
        <item name="android:colorAccent">@color/branco</item>
        <item name="android:colorPrimaryDark">@color/branco</item>

    </style>

    <style name="estiloParaEdtText" parent="AppTheme">
        <item name="android:textCursorDrawable">@color/branco</item>
    </style>

</resources>
